{
  "name": "cliproxyapi-dashboard-workspace",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "apps/*",
    "workers/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm --prefix apps/dashboard run dev",
    "build": "npm --prefix apps/dashboard run build",
    "start": "npm --prefix apps/dashboard run start",
    "test": "npm --prefix apps/dashboard run test",
    "typecheck": "npm --prefix apps/dashboard run typecheck",
    "lint": "npm --prefix apps/dashboard run lint",
    "build:collector": "npm --prefix apps/dashboard run build:collector"
  }
}
